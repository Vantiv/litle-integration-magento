<?php
/**
 * Custom tab template
 */
?>

<div class="grid"/>

<?php
$collection = Mage::getModel('palorus/insight')->getCollection();
if ($collection->count() >= 1) {
    // store the retrieved feeds
    $feedOutput = array();
    // iterate over the retrieved retrieved collection
    foreach ($collection as $flickrFeedItem) {
        $feedOutput[] = $flickrFeedItem->getData();
    }
}
var_dump($feedOutput); //TODO - delete me
?>


<table cellspacing="0" class="data">
            <thead>
                <tr class="headings">
		    <th>Order Id</th>
                    <th>Last 4</th>
                    <th>Affluence</th>
                    <th>prepaid</th>
                    <th>reloadable</th>
                    <th>available_balance</th>
                    <th>issuing_country</th>
                </tr>
            </thead>
        <?php foreach($feedOutput as $feed) { ?>
		<tr>
			<td><?php echo $feed['order_id']; ?></td>
			<td><?php echo $feed['last'] ?></td>
			<td><?php echo $feed['affluence']; ?></td>
			<td><?php echo $feed['prepaid']; ?></td>
			<td><?php echo $feed['reloadable'] ?></td>
			<td><?php echo $feed['available_balance']; ?></td>
			<td><?php echo $feed['issuing_country']; ?></td>
		</tr>
	<?php } ?>


</table>
</div>
